import React from 'react';
import { Container } from 'react-bootstrap';
import { Header } from 'components';

const Hola = (_) => {
  return (
    <Container fluid style={{ backgroundColor: '#FFE600' }}>
      <Header />
    </Container>
  );
};

export default Hola;
